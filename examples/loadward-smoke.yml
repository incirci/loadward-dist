name: Loadward smoke

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: isolated-local
    timeout-minutes: 5
    steps:
      - name: Verify runner
        run: |
          echo "Loadward runner is alive"
          uname -a
          id
